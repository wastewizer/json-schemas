{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "description": "A status updated message for a container site",
  "properties": {
    "msgID": {
      "type": "string",
      "description": "A static message ID in the format 000000000000000000000000-weight"
    },
    "msgVer": {
      "type": "string",
      "description": "The message version, e.g., 1.0"
    },
    "gwID": {
      "type": "string",
      "description": "The gateway ID that sent the message"
    },
    "d": {
      "type": "object",
      "description": "The data of the update",
      "properties": {
        "s": {
          "enum": ["ONLINE", "OFFLINE", "DAMAGED"],
          "description": "The status of the container site"
        },
        "sc": {
          "type": "number",
          "description": "The status code of the container site"
        }
      },
      "required": ["s", "sc"]
    },
    "ts": {
      "type": "string",
      "format": "date-time",
      "description": "The timestamp of the update in ISO 8601 format"
    }
  },
  "required": ["d", "ts"]
}
